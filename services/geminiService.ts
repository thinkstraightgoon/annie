import { InvestmentItem } from "../types";

// Service deprecated as AI features have been removed.
export const analyzePortfolio = async (data: InvestmentItem[]): Promise<string> => {
  return Promise.resolve("");
};